matchStart: -1
anyOf:
- type: dir
- match: a_(.*)
  rewrite: b_\1
  then:
    type: false
- match: b_(.*)
  rewrite: a_\1
  then:
    type: false
- match: (.*)
  then:
    anyOf:
    - rewrite: a_\1
      then:
        type: file
    - rewrite: b_\1
      then:
        type: file
